<h1> Lists </h1>
<% @pages.each do |list| %>
  <h2><%= list.title %></h2>
  <%= link_to 'Show List', list_path(list) %>
  <%= link_to 'New Task', new_list_path(list) %>
  <!-- <%= link_to 'Show Task', list_path(list) %> -->

<%= end %>

